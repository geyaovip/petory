/** Used when a default Petory sample pose image is attached as the second reference image (anchor mode only). */
export const POSE_REFERENCE_IMAGE_RULES = `You receive two reference images. Image 1 is the anchor rendering: keep this exact subject's identity, face, colors, markings, textures, proportions, rendering style, lighting, and apparent scale. Image 2 is pose choreography reference only: match its body pose, limb placement, head angle, expression type, camera distance, camera height, and three-quarter framing. Never copy species, colors, markings, face shape, fur, clothing, or any identity traits from image 2. The output must look like the subject in image 1 performing the pose shown in image 2.`
